---
- hosts: isma_gw_mts
  become: yes
  become_method: sudo
  gather_facts: no
  pre_tasks:
    - name: Install python
      raw: sudo apt install python-simplejson -y

  roles:
    - openvpn
    - shorewall
    - nginx
    - dns
    - dhcp
    - common
